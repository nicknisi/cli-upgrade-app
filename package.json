{
  "name": "@dojo/cli-upgrade-app",
  "version": "3.0.0",
  "description": "CLI command to upgrade Dojo applications",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "artifact:build": "cd test-app && npm run build:dist:evergreen && shx mv output/dist output/dist-evergreen  && npm run build:dev:evergreen && shx mv output/dev output/dev-evergreen && npm run build:dist && npm run build:dev && npm run build:dist:evergreen",
    "artifact:install": "cd test-app && shx rm -rf node_modules && npm i && npm run install-cli-upgrade-app",
    "artifact:package": "grunt dist && grunt release-publish-flat --dry-run && shx mv dist/dojo-cli-upgrade-app-* dist/dojo-cli-upgrade-app.tgz"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dojo/cli-upgrade-app.git"
  },
  "license": "BSD-3-Clause",
  "bugs": {
    "url": "https://github.com/dojo/cli-upgrade-app/issues"
  },
  "homepage": "https://github.com/dojo/cli-upgrade-app#readme",
  "devDependencies": {
    "@dojo/cli": "2.0.0",
    "@types/node": "9.6.5",
    "cypress": "3.0.1",
    "grunt": "1.0.1",
    "grunt-dojo2": "latest",
    "grunt-tslint": "5.0.1",
    "intern": "4.1.0",
    "prettier": "~1.13.7",
    "shx": "~0.3.2",
    "tslint": "~5.8.0",
    "typescript": "2.6.1"
  }
}
